// Database types generated from the Lovable Cloud (Supabase) schema.
// The source of truth is the auto-generated file at
// `src/integrations/supabase/types.ts`. This file re-exports those types
// under `src/types/` so the rest of the app can import database types from
// a single, stable location alongside the existing frontend domain types.
//
// Do NOT hand-edit the underlying generated file. To refresh the schema,
// re-run the Supabase type generation; this re-export will pick up the
// changes automatically.

export type {
  Database,
  Tables,
  TablesInsert,
  TablesUpdate,
  Enums,
  CompositeTypes,
  Json,
} from "@/integrations/supabase/types";
